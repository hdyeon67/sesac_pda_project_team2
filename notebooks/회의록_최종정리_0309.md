# 📋 프로젝트 최종 정리 회의록 (0309)

> 작성일: 2026년 3월 9일  
> 대상 파일: `01_eda.ipynb`, `Step5_1_ModelB.ipynb`, `resources/*`, 문서 파일

---

## 1. 오늘 최종 합의

- 메인 분석 흐름은 `Step 1~9`로 확정
- Step 8은 기존 단일 세그먼트 분석을 제거하고, `모델A Top3 채널 + 코호트 통합` 방식으로 확정
- Step 9는 `Step 9-1 ~ Step 9-4`로 분리해 발표/실행에 바로 쓰는 구조로 확정
- `Step5-1 모델B`는 메인 노트북에서 제외하고 별도 파일(`Step5_1_ModelB.ipynb`)로 보관

---

## 2. 01_eda.ipynb 최종 구조

- Step 1: 채널별 유입 규모
- Step 2: 채널별 유료전환율(메인 KPI)
- Step 3: 채널별 매출액 vs 유료전환율
- Step 4: 채널별 D7/D30 리텐션
- Step 5: 종합 스코어링 모델A(ROI 포함)
- Step 6: 연령대 × 채널 전환율
- Step 7: 디바이스 × 채널 전환율
- Step 8-1: 모델A Top3 채널 선정 + 코호트 점수 계산
- Step 8-2: 채널별 코호트 TOP5
- Step 8-3: 통합 코호트 결과표
- Step 8-4: 시각화
- Step 9-1: 최종 실행 요약표
- Step 9-2: 최종 우선순위 시각화
- Step 9-3: 최종 권고안
- Step 9-4: 채널별 액션 후보(최종)

---

## 3. 핵심 결과(현재 기준)

- Step 5 모델A Top3 채널: `referral`, `youtube`, `google_ads`
- Step 8 최우선 실행 세그먼트: `referral | 30-34 | web`
- Step 9 권장 예산 비중:
  - P1 `referral`: `51.1%`
  - P2 `youtube`: `25.5%`
  - P3 `google_ads`: `23.4%`

---

## 4. 산출물 저장 체계 정리

### 4-1) Step 8/9 전용 리소스

- 경로: `resources/step8_step9/`
- 표: `tables/`
- 시각화: `figures/`

### 4-2) Step 1~9 전체 리소스

- 경로: `resources/all_steps/step01 ~ step09/`
- 각 스텝별 `tables/`, `figures/` 분리 저장
- 자동 생성 스크립트: `scripts/export_all_steps_resources.py`

---

## 5. Git 반영 내역

- 원격 저장소: `https://github.com/hdyeon67/sesac_pda_project_team2.git`
- 브랜치: `main`
- 커밋: `50e42bf`
- 메시지: `Restructure Step9 and export Step1-9 resources`
- 상태: `origin/main` 푸시 완료

---

## 6. 다음 작업

1. Step 9 발표용 내러티브(슬라이드 문장) 확정
2. 채널별 액션 후보(9-4)에서 실행 난이도/기대효과 우선순위 부여
3. 최종 발표 슬라이드에 `Step 8-3 표 + Step 9-2 그래프`를 핵심 장표로 고정
